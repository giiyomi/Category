<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Category Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      max-width: 800px;
      background-color: #fff;
      padding: 50px 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h2 {
      margin:0px;
      color: #333;
    }

    p {
      font-size: 1.2rem;
      line-height: 1.6;
      color: #666;
    }

    .actions {
      margin-top: 40px;
    }

    .actions a {
      text-decoration: none;
      color: #007bff;
      margin: 0px 15px;
      font-size: 1.2rem;
    }

    .actions a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Showing <%= @category.name %>'s Details</h2>

    <p><strong>Name:</strong> <%= @category.name %></p>
    <p><strong>Description:</strong> <%= @category.description %></p>

    <div class="actions">
      <%= link_to "Back to Categories", categories_path %>
      <%= link_to "Edit this category", edit_category_path(@category) %>
      <%= link_to "Category's Task(s)", category_tasks_path(@category) %>
    </div>
  </div>
</body>
</html>
